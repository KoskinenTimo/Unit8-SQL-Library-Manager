extends ../layout

block content 
  h1 New Book 

  include validation
  
  form(action="/books/new", method="post")
    include form
    p
      input(type="submit", value="Create New Book")
    p       
      a.button(href="#") Cancel
      